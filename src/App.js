import React from 'react';
import {Text, View, SafeAreaView, ScrollView} from 'react-native';

const App = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>
          <Text>Its Work</Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
};

export default App;
